function task1() {                               // Оголошення функції task1
  let age;                                       // Змінна для збереження віку
  while (true) {                                 // Безкінечний цикл (вихід через break)
    age = prompt("Введіть ваш вік:");            // Запитуємо вік у користувача
    if (age === null) break;                     // Якщо натиснуто "Скасувати" — вихід
    age = parseInt(age);                         // Перетворення введення на ціле число
    if (!isNaN(age) && age >= 0) {               // Перевірка: число і не менше 0
      if (age <= 11) alert("Ви дитина.");        // Вік 0–11 → дитина
      else if (age <= 17) alert("Ви підліток."); // Вік 12–17 → підліток
      else if (age <= 59) alert("Ви дорослий."); // Вік 18–59 → дорослий
      else alert("Ви пенсіонер.");               // Вік 60+ → пенсіонер
      break;                                     // Вихід з циклу після успішного введення
    } else {
      alert("Некоректне значення. Спробуйте ще."); // Повідомлення, якщо введення не число
    }
  }
}

function task2() {                                            // Оголошення функції task2
  while (true) {                                              // Нескінченний цикл
    let digit = prompt("Введіть цифру від 0 до 9:");          // Запитуємо цифру
    if (digit === null) break;                                // Якщо натиснули "Скасувати" — вихід
    if (!isNaN(digit) && digit >= 0 && digit <= 9) {          // Перевірка: це число від 0 до 9
      switch (digit) {                                        // Вибір символа за цифрою
        case '0': alert("Символ: )"); break;                  // Якщо 0 → )
        case '1': alert("Символ: !"); break;                  // Якщо 1 → !
        case '2': alert("Символ: @"); break;                  // Якщо 2 → @
        case '3': alert("Символ: #"); break;                  // Якщо 3 → #
        case '4': alert("Символ: $"); break;                  // Якщо 4 → $
        case '5': alert("Символ: %"); break;                  // Якщо 5 → %
        case '6': alert("Символ: ^"); break;                  // Якщо 6 → ^
        case '7': alert("Символ: &"); break;                  // Якщо 7 → &
        case '8': alert("Символ: *"); break;                  // Якщо 8 → *
        case '9': alert("Символ: ("); break;                  // Якщо 9 → (
      }
      break;                                                  // Вихід після правильного введення
    } else {
      alert("Помилка: введіть цифру від 0 до 9.");            // Якщо не цифра — показати помилку
    }
  }
}

function task3() {                                            // Оголошення функції task3
  let start = parseInt(prompt("Початок діапазону:"));         // Введення початкового числа
  let end = parseInt(prompt("Кінець діапазону:"));            // Введення кінцевого числа
  let sum = 0;                                                // Змінна для зберігання суми
  if (!isNaN(start) && !isNaN(end)) {                         // Перевірка правильності обох чисел
    if (start > end) [start, end] = [end, start];             // Обмін місцями, якщо потрібно
    for (let i = start; i <= end; i++) sum += i;              // Цикл: додаємо всі числа в діапазоні
    alert(`Сума: ${sum}`);                                    // Виводимо результат
  } else {
    alert("Некоректні числа");                                // Повідомлення про помилку
  }
}

function task4() {                                            // Оголошення функції task4
  let a = parseInt(prompt("Перше число:"));                   // Введення першого числа
  let b = parseInt(prompt("Друге число:"));                   // Введення другого числа
  if (!isNaN(a) && !isNaN(b)) {                               // Перевірка обох чисел
    while (b !== 0) {                                         // Алгоритм Евкліда
      let temp = b;                                           // Збереження b у тимчасову змінну
      b = a % b;                                              // Знаходимо залишок від ділення
      a = temp;                                               // Присвоюємо a попереднє b
    }
    alert(`НСД: ${a}`);                                       // Вивід результату
  } else {
    alert("Помилка вводу");                                   // Повідомлення про помилку
  }
}

function task5() {                                            // Оголошення функції task5
  let number = parseInt(prompt("Введіть число:"));            // Введення числа
  if (!isNaN(number) && number > 0) {                         // Перевірка, чи число > 0
    let result = "";                                          // Змінна для збереження дільників
    for (let i = 1; i <= number; i++) {                       // Цикл від 1 до введеного числа
      if (number % i === 0) result += i + " ";                // Якщо ділиться — додаємо до результату
    }
    alert(`Дільники: ${result}`);                             // Виводимо всі дільники
  } else {
    alert("Некоректне число");                                // Повідомлення про помилку
  }
}

function task6() {                                            // Оголошення функції task6
  let num = prompt("Введіть п’ятизначне число:");             // Запит п’ятизначного числа
  if (num.length === 5 && !isNaN(num)) {                      // Перевірка: довжина = 5 та це число
    let isPal = true;                                         // Початково вважаємо, що паліндром
    for (let i = 0; i < num.length / 2; i++) {                // Цикл до середини числа
      if (num[i] !== num[num.length - 1 - i]) {               // Порівнюємо симетричні символи
        isPal = false;                                        // Якщо не рівні — не паліндром
        break;                                                // Перериваємо перевірку
      }
    }
    alert(isPal ? "Це паліндром" : "Це не паліндром");        // Виводимо результат
  } else {
    alert("Введіть п’ятизначне число");                       // Якщо умова не виконана — помилка
  }
}

function task7() {                                            // Оголошення функції task7
  let amount = parseFloat(prompt("Сума покупки:"));           // Введення суми
  let discount = 0;                                           // Змінна для знижки
  if (!isNaN(amount) && amount > 0) {                         // Перевірка: число > 0
    if (amount >= 500) discount = 7;                          // Якщо 500 і більше — 7%
    else if (amount >= 300) discount = 5;                     // Якщо 300–499 — 5%
    else if (amount >= 200) discount = 3;                     // Якщо 200–299 — 3%
    let final = amount - (amount * discount / 100);           // Обчислення остаточної ціни
    alert(`Знижка: ${discount}%\nДо оплати: ${final.toFixed(2)} грн`); // Вивід
  } else {
    alert("Некоректна сума");                                 // Повідомлення про помилку
  }
}

function task8() {                                            // Оголошення функції task8
  let positives = 0, negatives = 0, zeros = 0, even = 0, odd = 0; // Лічильники для всіх типів
  for (let i = 1; i <= 10; i++) {                             // 10 чисел
    let num = parseFloat(prompt(`Число #${i}:`));             // Запит числа
    if (isNaN(num)) continue;                                // Якщо не число — пропустити
    if (num > 0) positives++;                                // Додатнє
    else if (num < 0) negatives++;                           // Від’ємне
    else zeros++;                                            // Нуль
    if (num % 2 === 0) even++;                               // Парне
    else odd++;                                              // Непарне
  }
  alert(`Додатні: ${positives}\nВід’ємні: ${negatives}\nНулі: ${zeros}\nПарні: ${even}\nНепарні: ${odd}`); // Вивід
}

function task9() {                                            // Оголошення функції task9
  const days = ["Понеділок", "Вівторок", "Середа", "Четвер", "П’ятниця", "Субота", "Неділя"]; // Масив днів
  let i = 0;                                                  // Лічильник днів
  while (true) {                                              // Нескінченний цикл
    let next = confirm(`${days[i]}. Хочеш побачити наступний день?`); // Питання користувачу
    if (!next) break;                                         // Якщо "Скасувати" — вихід
    i = (i + 1) % days.length;                                // Переходимо до наступного дня (по колу)
  }
}